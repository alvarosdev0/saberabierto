# Specs Directory

Place domain-specific specification files here following the convention:

```
openspec/specs/{domain}/spec.md
```

See `openspec-convention.md` in the shared skill references for details.
